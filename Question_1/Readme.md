This directory has files for question 1 of the assignment <br />
To run the file ```comm.sh``` run the following commands on linux terminal <br />
```bash
chmod +x comm.sh
```
```bash
./comm.sh file1.txt file2.txt
```
